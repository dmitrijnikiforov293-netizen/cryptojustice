server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;
    charset utf-8;

    location / {
        try_files $uri $uri/ $uri/index.html $uri.html =404;
        autoindex off;
    }
}
